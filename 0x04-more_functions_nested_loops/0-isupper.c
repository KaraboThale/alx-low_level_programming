#include "main.h"

/**
*_isupper - Tests for uppercase letters
*@c: Input character
*Return: (1) if uppercase (0) for anything else
*/

int _isupper(int c);

{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
